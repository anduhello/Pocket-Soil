# 素材苗床 P0：范围基线与开发清单

日期：2026-09-18。状态：范围已确认，代码能力初查完成，尚未完成业务验收。

## 已确认的产品决策

- 首批用户：设计者与创作者。
- 一条收藏可以属于多个主题；P0 不开发独立项目模块。
- P0 手动整理主题、标签与备注；AI 自动整理进入 P1。
- P0 今日漫游包含推荐展示、打开原链接、跳过与反馈；不做提醒。
- 素材苗床独立运行，不共享 Hothouse 的私人数据。
- 打开原链接不等于已使用；读取状态与使用状态分开。

## 核对方法与证据边界

本次为静态代码初查，没有创建账号、导入收藏、运行业务测试或修改数据库。
“已有”只表示发现实现入口，不等于满足所有验收条件。未配置付费 AI 服务。

### 后续基础自动测试：2026-09-18

在开发容器内运行已有 tRPC 测试，5 个文件、144 个用例全部通过，退出码 0，耗时 47.09 秒。

| 测试文件 | 用例数 |
| --- | ---: |
| routers/bookmarks.test.ts | 43 |
| routers/lists.test.ts | 26 |
| routers/tags.test.ts | 30 |
| routers/importSessions.test.ts | 9 |
| routers/users.test.ts | 36 |

测试夹具使用 `getInMemoryDB(true)` 创建内存 SQLite，并生成测试用户；队列采用模拟实现。进程的 `DATA_DIR` 设为容器 `/tmp`，避免模块加载时打开 `/data` 收藏数据库。第一次使用尚不存在的临时目录，加载失败，未执行用例；改用已有 `/tmp` 后通过。没有在应用页面注册真实账号。

执行命令（在主机 PowerShell 中）：

```powershell
& 'C:\Program Files\Docker\Docker\resources\bin\docker.exe' exec -e DATA_DIR=/tmp -w /app/packages/trpc hothouse-dev-web-1 pnpm exec vitest run routers/bookmarks.test.ts routers/lists.test.ts routers/tags.test.ts routers/importSessions.test.ts routers/users.test.ts --maxWorkers=1
```

参数说明：`exec` 在现有容器启动单独进程；`-e` 只给该进程设置临时数据路径，不改变 Web 服务配置；`-w` 指定测试工作目录；`vitest run` 执行一次后退出；`--maxWorkers=1` 限制测试并发，减少对本地运行服务的资源争用。

结论仅适用于这些现有自动用例，包括已有隐私检查等；不等于产品 P0 全部通过。真实浏览器登录、抓取 Worker、图片权限、导出路由、搜索服务清理、并发重复提交、重新读取后手动字段保留及今日漫游仍需专项验收。上表中的“待测”是产品完整验收状态，不因基础测试通过自动改为完成。

## 需求—已有能力—差异—验收清单

| 需求 | 本地代码证据 | 差异与下一步 | 验收状态 |
| --- | --- | --- | --- |
| 注册、登录、退出、私人空间 | `packages/trpc/auth.ts`、`routers/users.ts`、`apps/web/app/signup`、`signin`、`logout` | 复用；补双账号直接 API 越权测试，覆盖图片、搜索、导出、任务结果 | 待测 |
| 单链接保存与后台读取 | `packages/trpc/routers/bookmarks.ts`、`apps/workers/workers` | 复用；核验失败后链接保留、重试、手动字段保护和状态提示 | 待测 |
| 重复链接控制 | `createBookmark` 已有去重分支；源码明确指出并发竞态不能完全防重 | 优先补并发防重方案；普通重复提交可能更新字段及归档状态，需要改为产品约定的明确提示、无静默覆盖 | 待改、待测 |
| 多主题、标签、备注 | `packages/db/schema.ts` 的 `bookmarksInLists` 为收藏与列表联合主键；标签与收藏也为关联表 | 用手动列表承载主题，不另建重复主题体系；核验权限、编辑与重新抓取后保留 | 待测 |
| HTML 导入与文件夹结构 | `apps/web/components/settings/ImportExport.tsx`、`lib/hooks/useBookmarkImport.ts`、`packages/shared/import-export` | 复用解析与导入能力；当前选择文件后进入导入调用，需增加预览与明确确认，确认前不创建正式收藏或主题 | 待改、待测 |
| 批量粘贴与预览 | 可复用导入暂存接口 `stageImportedBookmarks`、`finalizeImportStaging` | 新增批量粘贴入口；统一有效、已存在、文件内重复、无效与超额计数，服务器复核 | 待开发 |
| 后台任务与逐条结果 | `routers/importSessions.ts`、`models/importSessions.service.ts`、`apps/workers/workers/importWorker.ts` | 已有暂存、启动、暂停、恢复和分页结果；补失败项重试，明确导入结果与读取结果区别 | 待改、待测 |
| 搜索、筛选、分页 | `routers/bookmarks.ts`、`models/bookmarks.ts`、`lib/search.ts` | 复用搜索体系；普通列表查询对部分筛选组合有限制，需核验 UI 所需组合，不直接承诺全部筛选共同生效 | 待测 |
| 已使用状态 | 已有归档及阅读进度，不能直接等同产品的“已使用” | 明确单独状态及迁移方案，不把打开、归档、读取完成偷换成已使用 | 待开发 |
| 今日漫游 | 本次检索未发现符合需求的推荐、冷却及反馈闭环 | 新增本人收藏候选、随机抽取、跳过反馈、重复抑制和真实推荐理由；空状态明确 | 待开发 |
| 导出、删除、账号删除 | `apps/web/app/api/bookmarks/export/route.tsx` 支持 JSON；导出 UI 包含 HTML；`routers/users.ts` 有账号删除 | 核验 HTML 转义、多主题导出还原、删除后索引与资源清理、账号删除和备份保留规则 | 待测 |
| 抓取安全与额度 | `apps/workers/network.ts` 有 URL 校验及重定向校验；存在额度与限流代码 | 不能据此认定安全完备；测试内网、危险跳转、子请求及 DNS 情况；补导入每日额度、文件大小、并发与服务端总量控制 | 待测、待补 |
| 对外上线 | 当前为 localhost Docker 开发环境 | 另做生产配置、HTTPS、备份恢复、监控、隐私条款与回滚；本清单不授权开放公网 | 未上线 |

## 小批次开发顺序

### 已实施的第四项修改：批量粘贴链接（2026-09-18）

入口：设置 → 导入／导出 → 批量粘贴链接。一行一个完整地址，忽略空行，支持常见换行格式。点“预览链接”后复用本人查重与确认弹窗；仅新增有效条目进入后台任务。取消和报错保留输入，成功暂存并启动任务后清空；处理中禁用输入和按钮，预览内显示“本次输入重复”。链接标题留空，避免把网址当成用户手动标题挡住后续元数据展示。

实现使用 `links` 解析来源及现有导入器，不伪造 HTML，不展开平台列表，不下载视频。复用项目 React 与 shadcn 的 Card、Field 和 Textarea，提供明确标签与说明。未新增依赖、数据库迁移或每日额度规则。

同时修正 HTML 原始重复统计：`parseImportFile` 新增可选 `preserveDuplicates`，预览调用开启，默认调用仍保留上游去重合并行为。新增实际解析到预览的测试，避免只测试分类函数遗漏前置合并。

验证：解析器 30、导入器 8、预览 5，共 43 用例通过；前端 TypeScript 检查通过。未完成登录后的浏览器端全流程验收，不能据自动测试承诺所有站点抓取成功。上一版本 Git 快照为 `cd58c43`，本批修改尚未提交。

### 已实施的第三项修改：HTML 书签导入预览（2026-09-18）

入口：设置 → 导入／导出 → HTML File。选择文件后先解析，再通过 `previewExistingLinks` 分批查询当前用户已有的相同地址；弹窗显示新增、已收藏、文件内重复、无效四类及原路径。计数包含全部条目，列表显示前 100 条。

只有确认后才调用原导入器创建主题、任务与收藏。取消、关闭弹窗及等待确认时离开页面，均拒绝确认；同一导入组件只允许一个文件流程。已有、无效及文件内重复条目不进入本次新增导入。现阶段重复条目的其他文件夹路径不会合并进原收藏，也不会覆盖原内容。

复用既有 React、shadcn 弹窗及按钮，保留弹窗标题、描述、键盘关闭行为，不安装或覆盖 UI 组件。文件元数据以文本显示，不执行导入 HTML。

验证：预览分类、保留路径、不合并不同查询参数、取消不调用写入依赖及导入器回归共 11 用例通过；收藏与新增预览接口权限测试共 45 用例通过。前端 `tsc --noEmit` 通过，7 个改动文件 oxlint 为 0 警告、0 错误，`git diff --check` 通过。尚未完成登录后的浏览器端交互验收。

范围：仅 HTML 文件入口启用预览；其他上游文件入口暂保留原流程。未新增批量粘贴、文件大小／每日总额度规则或全局地址规范化。地址格式校验仅接受无嵌入凭据的 HTTP(S)，不是网站可访问性或 SSRF 安全验收。预览与执行间数据可能变化，创建阶段仍再次防重。数据库结构不变。

### 已实施的第二项修改：创建事务内防重（2026-09-18）

在 SQLite `immediate` 写事务内部再次检查同一用户、同一链接地址，若已存在则返回已有收藏，不再创建或排入抓取队列。保留事务外快速检查。检查与插入之间无异步操作，由写事务保护创建路径；不同用户仍可分别收藏。

新增 8 请求、2 用户的并发测试：每人只生成一条、总共两条，只有新增条目入队。收藏与导入共 53 用例通过，后端 TypeScript 类型检查通过。初次队列断言引用错误模拟，修正为测试夹具的队列引用后通过。

范围限制：当前仅比较去除首尾空白后的相同地址，没有删除查询参数、追踪参数或片段，也不合并跳转地址。不会清理历史重复数据。该保护适用于本次修改的创建入口，不是数据库全局唯一约束；其他直接写入或修改地址入口仍需核验。多进程写入争用及忙超时也需独立压测。

### 已实施的第一项修改：重复创建不覆盖（2026-09-18）

`packages/trpc/routers/bookmarks.ts`：同一用户重复创建已匹配的链接时，直接返回已有收藏及 `alreadyExists: true`，不覆盖标题、备注、摘要、收藏标记，不更改归档状态与时间，不触发本次重复操作的编辑 webhook 或搜索重建。明确编辑仍走 `updateBookmark`。

更新了上游两条重存测试，使其验证本产品的无覆盖规则，并验证明确编辑仍然可用。收藏与导入测试共 52 个用例通过；修改的两个文件 oxlint 检查为 0 警告、0 错误。

该改动未涉及数据库迁移。并发竞态防重、地址规范化和前端重复提示仍未完成，不能据此承诺所有重复输入都已解决。已有能力矩阵保留历史初查结果。

1. 基线验证：双账号隔离、单链接保存、读取失败、手动修改、多主题、导出删除。先复现再修改。
2. 导入闭环：安全校验、保守地址规范化、并发防重、预览确认、批量粘贴、额度及逐条失败重试。
3. 再发现闭环：使用状态、今日漫游接口与页面、反馈及重复抑制；打开不自动标记已使用。
4. 产品收口：主题用语与导航、面向设计者的精简界面、移动端和可访问性。视觉方案确认后修改。
5. 上线准备：恢复演练、隐私和来源规则审查、生产安全配置及上线验收。

每批应包含代码、必要迁移、测试、操作说明和实际验证结果。保留既有源码功能，先用入口收口与配置控制，不贸然删除共享代码。

## 技术与数据约束

- 继续复用 Karakeep 的 TypeScript、Next.js、tRPC、SQLite、后台 Worker 与搜索服务；暂不引入 Java 或 Python 第二套后端。
- 数据库变更使用迁移，先备份并验证恢复，不手工删除数据库或 Docker 数据卷。
- 公开列表、协作、全文归档等上游能力不属于本产品默认开放范围，正式上线前逐项核对开关和 API 行为。
- 不获取用户原平台密码或 Cookie，不绕过登录、验证码及付费墙，不托管完整视频。
- AI 功能 P1；基础保存、读取、手动整理不得依赖 AI 成功。
- 保留上游许可证及版权文件；发布前单独核对二次开发的许可证履行方式。
- 导入数量、文件大小和每日额度尚未最终确认，讨论稿数值不视为已实现规则。

## 给新开发者的原理说明

多主题通过关联表实现：收藏保存一份，关联表保存“收藏 ID—主题 ID”，加入新主题只是新增关联，不复制内容。

后台任务通过数据库状态与 Worker 执行：网页负责提交和查询进度，Worker 负责处理。完成暂存并启动后，关闭网页不应终止后台任务；上传暂存尚未完成时关闭网页则另当别论。

去重不能只靠“先查询再新增”：两个请求可能同时查到不存在，再分别新增。需要在数据库约束或可靠的事务、串行执行设计中保证同一用户同一规范化地址的唯一性，并处理冲突结果。

安全不能只靠隐藏按钮：前端只是交互，服务器必须验证请求人是否有权操作目标数据。测试需要直接发请求模拟修改 ID 的情况。
