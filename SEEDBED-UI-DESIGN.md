# 素材苗床界面改版

用户已确认两张设计预览，沿用现有 Next.js、shadcn、i18next 和服务端登录检查，不增加另一套前端或数据库。

## 设计基准

- 主界面参考：`C:/Users/y/.codex/generated_images/01a09523-6fde-7140-ac27-0433e7e23fcb/exec-48a20224-431a-4b45-9dd4-bbfd610ff6d8.png`。
- 弹窗参考：`C:/Users/y/.codex/generated_images/01a09523-6fde-7140-ac27-0433e7e23fcb/exec-a221bf89-19f0-48a5-a3a9-28525661f293.png`。
- 浅绿白背景 #fbfcf6，白色内容卡片，橄榄绿 #637f2e 主操作，深绿色 #233023 正文，边框 #e4e8dd；统一通过语义 CSS 变量实现。
- 桌面侧栏 220px，顶栏 64px；页面内边距移动端 16px、桌面 32px；导航和按钮 14px，页面标题 30px。
- 既有 Lucide 线框图标；卡片约 14px 圆角，操作和输入框沿用组件半径比例；不使用渐变、装饰性植物或假统计。
- LibraryHeader 管理添加弹窗与导入入口，Header 管理搜索和语言入口，LanguageSwitcher 保存浏览器语言偏好；现有收藏网格和后台导入逻辑继续负责业务。

## 行为与有意差异

- 中英界面切换通过现有设置 Cookie 保存，刷新服务端页面；不翻译或覆盖收藏原文，也不改变登录凭据。
- 新浏览器默认中文、浅色、网格；用户已有语言、布局和主题设置不强制清空。
- 示例封面、时长和数量不是产品种子数据，不导入用户数据库；视频继续打开原网站，不做站内播放器。
- 没有准确视频/图文元数据的情况下不增加无效快捷筛选；保留实际可用的搜索、主题导航、布局与排序操作。这部分与概念图不同，后续按真实内容类型能力补齐。
- 单条添加复用 EditorCard；批量导入进入现有导入页，预览列出新链接、已收藏、输入重复和无效地址。取消不创建数据。
- 原上游完整注册、错误提示和其他次级页面的翻译覆盖仍需逐页审查；本批不宣称全站文案均已本地化。

## 验收状态

代码已实现主要结构；前端类型检查通过，导入回归 43 项通过，语言偏好/字典/独立实例测试 5 项通过。登录页中文和英文切换、刷新保留已在 IAB 操作；测试发现原全局语言实例造成 hydration 不一致和渲染期更新警告，已改为服务端传入字典、页面独立同步实例，需继续浏览器复核。尚未取得已登录浏览器进行主界面和导入弹窗的完整视觉、移动端及交互验收，因此不能标为设计验收完成。

对照清单：背景/卡片色、侧栏宽度、顶栏搜索与语言位置、标题和主操作层级、卡片封面及外链按钮、弹窗预览表格及确认/取消状态。真实收藏数量和图片不要求与示例图一致。

浏览器复核：修复后新页面的中英切换未出现此前的 hydration 和渲染期更新警告。另发现 Next.js 开发缓存导致 API 路由返回 404；停前端后将 `.next/dev` 保留到 `.next/dev-before-ui-20260918`，重新启动后会话 API 恢复 200 JSON。该目录属于生成缓存、仍由 `.next/` 忽略规则排除；未删除数据库或源码。
