# B 站收藏夹与可选 AI 标签

## 使用

打开 http://localhost:3000/settings/import，粘贴带 fid 的公开收藏夹地址。
默认“读取并自动生成标签”，也可选择“仅读取，不使用 AI”。确认后逐条导入标题与原视频链接；新 B站视频采用轻量保存，不再打开网页、截图或下载封面。
简介来自收藏夹接口，保存在收藏备注中，可能被平台截断；不是视频内容总结。
接口可能少返回失效或不可用条目。预览显示平台总数和实际读取数，不补造数据。
最多读取 500 条，每用户每分钟最多读取 3 次；超限明确提示不是完整导入。
部分分页失败时不返回看似完整的结果，也不自动创建收藏。

## AI 配置（管理员本地操作）

在项目根目录 `.env.seedbed-ai.local` 填写：

```dotenv
SEEDBED_AI_API_KEY=你的模型服务API密钥
SEEDBED_AI_BASE_URL=https://api.deepseek.com
SEEDBED_AI_MODEL=deepseek-flash
```

这不是终端命令，而是服务器环境变量配置。API_KEY 用于服务认证，BASE_URL 决定请求发往哪里，MODEL 指定模型。
DeepSeek 官方文档：https://api-docs.deepseek.com/ 。更换兼容供应商时一起更换地址、模型与密钥。
密钥不要放入前端代码、聊天或提交 Git；`.env*.local` 已被忽略。不要运行会打印全部 Compose 配置的命令分享日志。

填写并保存后，在项目根目录 PowerShell 执行：

```powershell
& 'C:\Program Files\Docker\Docker\resources\bin\docker.exe' compose --project-name hothouse-dev --env-file .env.docker.local -f docker/docker-compose.dev.yml -f docker/docker-compose.hothouse.yml up -d --no-deps web
```

这条命令按现有配置重新创建网页容器，从新的配置文件加载密钥；单纯浏览器刷新不能更新容器环境变量。
`--no-deps` 不重建数据库等依赖；没有删除数据卷，不清空收藏。

## AI 行为与限制

默认发送新增视频标题（500 字符以内）、简介（1200 字符以内）和当前用户最常用的最多 200 个标签名称，优先复用标准名称，没有适用项时建议新标签。每批最多 50 条，分批处理新增视频。
不发送视频网址、B 站 Cookie 或用户密码。整理窗口中的 AI 按钮会使用该收藏的标题和备注简介，界面明确披露发送范围；不要把不希望发送给模型的内容放入备注。没有密钥时不请求模型，并允许继续基础导入。
每用户每日最多 100 次标签请求，每次输出最多 3000 tokens；不自动重试，取消导入或取消勾选不能撤销已经发生的模型费用。逐张整理的一次 AI 匹配也计入该额度。
已有收藏不参与这次标签生成。建议先在预览中展示，确认导入视为接受建议，保存为可编辑标签，不覆盖已有收藏。
返回标签数量、长度、字符及条目 ID 均校验；AI 故障不阻止基础导入。

这不是完整计费系统：上线前仍需全站预算、使用量审计、告警与额度持久化。API 格式兼容不保证每个模型都支持 JSON 输出。
已完成单条 DeepSeek 实连测试；后续回归测试使用模拟服务，不自动产生费用。

## 逐张整理与当前边界

收藏库和列表提供“整理未标记收藏”和“逐张整理（当前已加载）”。仅包含当前账号拥有的、已加载的 B站视频链接，不自动拉取整个收藏库。需要更多条目时先加载更多。
AI 结果先合并到当前草稿，手动输入可复用常用标签。确认保存后新增标签加入个人库，随后进入下一条；跳过或退出不保存当前草稿。删除需要再次确认，只删除素材苗床收藏，不删除原网站内容。
现有标签管理保留重命名和合并能力；这轮不提供独立语义别名表、合并撤销/审计记录，也不承诺 AI 语义去重百分之百正确。
导入页显示 Bilibili、小红书和抖音来源及中性来源图标。只有 B站公开收藏夹展开和 AI 匹配已接入；其他两个平台仅提供通用链接保存入口，未实现收藏夹抓取或平台自动标签匹配。
